# Sweep execution progress (branch: mamps/sweep-fixes)

Spec: docs/SWEEP-2026-06-03.md. Each set: implement → tests → /simplify → /review-branch → address → commit.

- [x] Set A — Class 3 (committed: "Unify check-flight verdict logic with timezone-aware date matching"; simplify 4-agent + review 10 findings all addressed; 260 tests): unify 4 check-flight impls, departure-local date window, negative-status check in FR24 fallback + boundary tests
- [x] Set B — Class 2 (committed: "Fail closed on upstream errors..."; simplify 11 items + review 10 findings incl. 4 self-regressions all addressed; 295 tests; deferred: alaska outage circuit-breaker → Set H, fr24 client injection → Set I): fail-open → fail-closed (alaska-verifier touch-on-failure, sheet DELETE sanity floor, FR24 catch{[]}, alaska-status "None", QR catch-all negative, qatar ingester meta stamp) + error-path table tests
- [x] Set C — Class 1 (committed f5f628d; simplify 14 + review 10 all addressed; 344 tests; marketing-prefix-only hub detection — QXE coverage loss accepted; hub /api/data fleetStats:null pinned): kill `?? AIRLINES.UA`/`?? hub` defaults, required props, exhaustive CONTENT, fail-closed hub APIs, llms.txt fixes + registry-generated site×endpoint matrix test
- [x] Set D — Class 10 (committed; suite hermetic — 0 FR24 launches, 1.1s; per-checkout .test-snapshot.sqlite; goldens structural + ungated; .dockerignore closes .env/git/node_modules bake-in; 345 tests): TEST_DB derived from repo root, contract tests through dispatch, golden visible-skip
- [x] Set E — Class 4 (committed; 406 tests; registry owns vocabulary; type-settled≠verified — DEPLOY NOTE: one-time prod cleanup needed — the pre-fix seed runs (ha_seed/as_seed) stamped the same type-rule verdicts as type_deterministic, and consensus (OBSERVED_WIFI_SOURCES=['united']) can never heal them: UPDATE starlink_planes SET DateFound=NULL, verified_wifi=NULL, verified_at=NULL WHERE sheet_gid IN ('type_deterministic','ha_seed','as_seed'); UPDATE united_fleet SET verified_wifi=NULL, verified_at=NULL WHERE tail_number IN (SELECT TailNumber FROM starlink_planes WHERE sheet_gid IN ('type_deterministic','ha_seed','as_seed')); HA/AS now type-settle on first reconcile with NULL stamps): registry-derived vocabulary (normalizeFleet horizon, A330/717 families, tail regexes, type→wifi single table, consensus sources, hosts dedupe, og-images glob)
- [x] Set F — Class 5 (committed; 439 tests; flightHistoryModel capability gate; type_split for bimodal fleets; UA shims deleted, guardrail ratcheted to 0; REST==MCP pinned per scope; telemetry note: AS/HA no-data lookups now tag predicted/low — DD mix shifts): MCP cfg threading (ensureAirlinePrefix, cfg prose, refuse non-UA predictions) + 404 POST /mcp where mcpPage false + per-scope goldens
- [x] Set G — Classes 6+7 (committed; 471 tests; review fixed: www 301 GET/HEAD-only, OPTIONS behind limiter+metrics, registry-membership redirect — www.localhost no longer bounces to prod; 468 tests; SITE_PAGES drives sitemap/llms/robots; robots now ALLOWS /mcp where mcpPage on — GSC coherence; www→apex 301 in dispatch; finalizeResponse fills nosniff/HSTS/XFO/Referrer-Policy/CSP-default-src-none + Vary: Host where absent; OPTIONS 204 on /api/* + /mcp, MCP responses carry CORS; dead siteManifest deleted, live manifest stays pre-421 for unknown hosts): tests/meta-surfaces.test.ts pins llms+sitemap URLs <400, sitemap∩robots=∅, headers×routes×sites, redirects
- [x] Set H — Class 8 (committed 99c7168; 486 tests; one startJob runner (src/utils/job-runner.ts: overlap skip, stuck-run abandon w/ token semantics, throw-safe, injectable clock) now drives all 11 loops — the 8 named jobs plus archive_departures, prune_crash_rows, data_freshness; cadences/initial delays preserved; qatar stuckTimeoutMs=2×interval since a full-outage run can exceed 1h; checkNewPlanes enqueue-only (returns queued count, no FR24, trickle picks up last_flight_check=0 first); refreshFleetMeta skips lastUpdated where registry lastUpdatedOwner='schedule-ingester' (QR — also covers residential-sync's third-writer path); qatar_ingester freshness gauge MAX(last_updated) + FRESHNESS_COVERAGE registry test; alaska job-level outage breaker 5 failures → skip 10 ticks): tests/jobs.test.ts. Adversarial round (5 applied + 5 refuted, 492 tests): QR gauge sentinel (empty table → meta fallback → epoch-0, never mute); ctx.isCurrent() so abandoned runs can't feed counters/breakers; per-airline alaska breakers (skip after selection — rotation proceeds); SIGTERM/SIGINT → stop all JobHandles; stampLastUpdated(db, code, writer) owner check at all three writers (registry: UA=sheet-scrape, QR=schedule-ingester) replaces fleet-sync's hard-coded UA skip; checkNewPlanes db required; generic tri-state breaker lives in job-runner; coverage test executes queries over seeded rows (note: NOT raw snapshot — its departure_log is empty and verifier non-null rows are UA-only)
- [x] Set I — Class 9 (committed 53aebe2; 538 tests; pure united-verdict.ts, 14-row matrix incl. adversarial error-with-payload fixture; logger tags preserved; clock seam threaded; verify-tails third ladder deleted): extract pure classifyCheckResult/applyUnitedObservation, inject checker, og script import.meta.main guard + verdict matrix table tests
- [x] Set J — Standalones 1-9 (committed db81059; 560 tests; review fixed QR-disable alert + buildRoster operator clobber + route-cache loss + subpage JSON-LD; escapeHtmlAttr boundary added) (renderHtml single-pass, canonicalPath template var, predictor noise filter, ACA/PDT/ENY prefixes, INSTALL_FILTER, fleet-sync roster, alaska tz +1 retry, dead enum docs, flight-data-fixer delete)
- [x] Final: 560 tests green; e2e 35/35 against live server (5 hosts × pages/SEO/APIs/MCP/redirects/headers/statics)
- [x] Cross-set integration review (whole-branch): 10 confirmed findings fixed (b06798e) — tenant-scoped tail ladder + foreign-prefix 404, guarded dispatch egress, zombie-run write gates, hub MCP carrier detection, type-derived≠verified reads, fr24Error threading, seed evidence tiers, verifier freshness, limiter coverage (mcp/permalinks), telemetry daysOut; + Horizon penetrationOverride; 586 tests, e2e re-passed 35/35 + cross-set probes
- [x] Consolidation round (3d819be): decideCarrier single decision core (REST+MCP renderers), pinned-MCP foreign-prefix refusal accepted + brand-clean text, runtime evidence guard, brand-isolation canaries restored; 593 tests, e2e 35/35 + refusal probes
